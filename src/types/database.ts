// Tipos generados desde Supabase.
// Cuando el proyecto Supabase esté creado, regenerar con:
//   npx supabase gen types typescript --project-id <ID> > src/types/database.ts

export type Database = Record<string, never>;
